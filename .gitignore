# build artifacts and executables
# /sipnet instead of just sipnet to NOT ignore test dirs
*.o
*.a
/sipnet
subsetData
transpose
.doxygen.stamp
.mkdocs.stamp
site/*
site_preview/*

# documentation
docs/api
docs/latex

# Test files
tests/sipnet/*/*
!tests/sipnet/*/*.c
!tests/sipnet/*/*.h
!tests/sipnet/*/*.in
!tests/sipnet/*/*.out
!tests/sipnet/*/*.clim
!tests/sipnet/*/*.param

# System-specific files
.DS_Store
.Rproj.user
.Rhistory
# Editor configs
src/sipnet/.vscode/*

# Smoke tests directory (ignored)
# Temporary and backup files
# Ensure `sipnet.out` files (authoritative model outputs) are never ignored
!**/sipnet.out
*.bak
*~
*.tmp
